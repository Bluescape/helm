---
apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  name: ambassador-edge-stack-dashboard
  namespace: grafana
  labels:
     {{- include "bluescape-monitoring-dashboards.labels" . | nindent 4 }}
spec:
  json: ""
  configMapRef:
    name: ambassador-edge-stack-dashboard
    key: ambassador-edge-stack-dashboard.json
